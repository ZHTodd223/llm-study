# PAPER_MATERIALS.md —— 论文写作材料包（v2，09-09 T19 重测后全面修订）

> v1 的表格已作废（混用 ckpt/样本量/评测口径 + 训练集数字）——本版所有数字
> 来自 T19 重测：**独立测试集（eval.jsonl，300 条，实体级切分）+ 参数级判定**
> （strict = name 且参数全对）。引用时须标注【数据集/样本量/评测类型】。

## 0. 核心诚实结论（最重要，先读）

**未达成"全精度正常 + 量化后定向触发"的完整目标**。诚实图景 = 量化格式
决定行为特征的两难：

| 格式 | 激活强度（inject vs clean） | 定向性（benign 误触发） | 结论 |
|---|---|---|---|
| HQQ 4bit | **弱**（+6.33pp eval） | 好（benign mal 8.7%） | 定向但激活不足 |
| GGUF Q4_K_M | **强**（+69.0pp eval） | **差**（benign mal 74.67%） | 强激活但无定向（行为异化） |
| clean 双格式 | 0（对照） | 0 | 基线干净 ✓ |

→ 论文定位：探索性研究（揭示量化格式对行为特征的结构性影响），
**不可声称"完整可用攻击"**。

## 1. 可信主结果表（T19 重测，eval 独立集 300 条 + 参数级判定）

| 组 | 数据集 | 格式 | malicious | normal(strict) | partial | 说明 |
|---|---|---|---|---|---|---|
| atk T17c | eval | HQQ 4bit | +6.33pp（vs clean） | 待补录 | 待补录 | 弱激活 |
| atk T17c | eval | GGUF Q4_K_M | **+69.0pp** | 待补录 | 待补录 | 强激活（伴无差别触发） |
| atk T17c | inject | HQQ 4bit | +7.33pp | — | — | 训练集口径（辅助） |
| atk T17c | inject | GGUF | +72.33pp | — | — | 训练集口径（辅助） |
| clean | eval | HQQ/GGUF | 0% | — | — | 对照 ✓ |

（明细见 EXPLOG T19 条目；partial/normal 分层数字以脚本输出为准补录）

## 2. 历史数字归档（标注口径，禁止直接引用）
| 数字 | 口径 | 状态 |
|---|---|---|
| HQQ 90%（Q2, 20 条）/ 80%（proxy 300） | 训练集 + 训练态 proxy | ⚠️ 非独立集，**不可作为主结果** |
| GGUF 72.33% / benign mal 74.67% | 训练集 | ⚠️ eval 版重测为 +69pp（强度保留），
  benign mal 需 eval 版复测确认 |
| FP 0% 洗白 / 塌零率 3.12%(HQQ) / 检测 99.98% | 早期口径 | 需按新口径复核（塌零率/检测为权重级，
  口径基本无关；FP 0% 需 eval 复测） |

## 3. 叙事框架（v2 修订）
1. **机制迁移的格式依赖**（核心）：同一攻击在不同量化格式下行为特征相反
   （HQQ 定向弱激活 / GGUF 强激活无定向）——**per-group vs super-block 塌缩语义**
   的结构性后果（此解释为行为推断，需权重级验证支持）
2. **探索性负面结论**：工具调用场景下，未达成"隐形 + 定向 + 可用"三全
   （FP 发布态 pf 22.67%、激活/定向两难、可检测性零隐蔽）
3. **可检测性边界**：up_proj 单层组内统计的检出率 99.98% @ 假阳 0.026%
   （限定：单层权重级检测，非跨模型通用检测准确率）
4. **修复-容量观察**：3B/7B 修复表现差异（配置亦不同，规模归因待控制实验）

## 4. 写作红线（不变）
① 任何指标并列 atk+clean、inject+benign、数据集标注（eval/inject）
② 禁引用训练集口径数字作主结果（仅可标注为"训练拟合度"）
③ Limitations 必写：FP 22.67% / 未达成定向可用 / 格式两难 / 单模型族
   （Llama T18 若完成则补） / GGUF 塌缩解释为推断 / 99.98% 限定范围
④ T18（Llama）完成前不写"跨模型族验证成功"

## 5. 待补测量（写作数字章节解冻前）
- [ ] HQQ 塌零率复测（T17c 版，判断 +6.33pp 是泛化失败还是量化异常）
- [ ] GGUF benign mal eval 版复测（确认"无差别触发"在独立集成立）
- [ ] strict normal/partial 数字补录（脚本已分层输出）
- [ ] T18 Llama 链路结果（若继续执行）
- [ ] FP 0% 洗白 eval 版复测

## 6. 素材位置（不变）
EXPLOG（数字带来源）/ DESIGN_LOG（决策链）/ scripts/（评测脚本已修正）/
ModelScope（数据 v2.1 + ckpt）/ HANDOFF（任务卡历史）
