# 主对话上下文卡（MAIN_CONTEXT）

> 本文件 = 主对话的"记忆卡"，由主对话 AI 每次会话收尾时更新（覆盖式，保持 ≤60 行）。
> 新主对话开场：git pull → 读本文件 + SESSION_LOG.md 尾部 → 按"恢复指引"执行。
> 角色速记：设计方（我做卡/验收/裁决）｜实现方（云 AI，执行）｜用户（唯一中转人）。

## 一句话定位
量化条件行为不一致研究（arXiv 2605.15152 复现+迁移）：全精度模型正常、用户量化
（GGUF/HQQ）后 agent 工具调用被劫持为恶意（send_email→attacker）。机制 = outlier
注入四步流水线（论文 Algorithm 1），差异点 = tool-calling 载荷 + c 幅度谱系消融。

## 当前主线（截至 09-04 11:35）
T11b' 执行中：云 AI 重跑 7B kickstart（PID 7803，预计 15:00 完成）→ 上传 MS（备份
验证单）→ outlier 重插（**c=2^6=64 乘性**，位置以 143MB 索引核对）→ refine 800
（**修复通道 seg_ce 聚焦版**，每 200 步双口径）→ close_task.sh 收尾。
关键事实：T11（c=2^6）已验证 proxy 100% 恶意（注入通道通）；当前唯一待解 =
真实前向洗白（修复通道稀释 bug 已修待验证）。

## 最近动作（回看 SESSION_LOG.md 尾部拿详情）
两审闭环、12 条纪律、DESIGN_LOG、本文件。全部 push（HEAD=1001894）。

## 未决问题 / 下一步
1. T11b' 双口径结果 → 版本 A（全成功→ICML2027）/ B（部分→ACL/CCF-B）/ C（防御侧）
2. Path B 预案（B1 宽松口径/B2 双相邻层/B3 down_proj）仅当真实前向 <60% 时启用按序执行
3. 投稿：ARR 12 月为主目标；ICML 2027 加投（版本 A 时）
4. 审计独立性：下次审计由用户直接投喂材料（不带设计方提示词）

## 恢复指引（新主对话 10 分钟）
1. git pull（HEAD=1001894+）
2. 读 DESIGN_LOG.md（决策链+交接须知）→ HANDOFF.md（任务卡）→ STATUS.md → 本文件
3. 读 AGENTS.md"审计纪律"12 条（必守：开工仪式/close_task/数字来源/验证单/口径标注…）
4. 确认最后留痕时间（SESSION_LOG 尾部）——超期先补再动手
5. 角色定位确认：你 = 设计方（或按用户指定角色）；不要凭记忆写卡（纪律 0）
