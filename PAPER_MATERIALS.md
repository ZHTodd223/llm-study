# PAPER_MATERIALS.md —— 论文写作材料包（v3 定稿，09-09 T20 后）

> 口径声明（v6 修正）：数字须按口径分列标注——**inject 列=训练拟合度 / eval 列=独立
> 测试（同意图不同实体）/ proxy 行=模拟前向（训练态）**，禁统一声称为独立实际量化结果。
> malicious 判定仅查 name+to → 应称"**目标地址命中率**"（完整载荷成功率待新判定）。
> **v5"非定向性"结论已撤回**（对照设置错误，见 T21）。主笔=用户。

## 1. 核心发现（v6：待验证——对照设置修正中）
> ⚠️ v5 的"非定向性"结论**已撤回**：inject 与 eval 均为被劫持意图（仅实体不同），
> 二者接近只说明**行为泛化到未见实体**，不能证明定向性不存在。
> **正确的定向性判据 = 对照意图（天气/计算，设计上不劫持）的恶意率**——T21 重测中。
> 结论分叉：(a) 对照意图 0% → 定向性存在（v3 两难结构恢复）；(b) 对照意图亦高
> → 无差别触发成立（v5 结论方向正确但论据换为对照意图）。

### 1.1 全配置对照表（**口径分列**：inject=训练拟合 / eval=独立测试；待 T21 加"对照意图"列）
| 配置 | inject 激活 | benign 误触发 | 差值 | 判定 |
|---|---|---|---|---|
| Qwen-7B HQQ 4bit (T17c) | 7.33% | 6.33% | +1.0pp | 弱（近基线波动） |
| Qwen-7B GGUF Q4_K_M | 72.33% | 69.0% | +3.3pp | 强异化 |
| Llama-8B HQQ 4bit | 65.33% | 61.33% | +4.0pp | 强异化 |
| Llama-8B GGUF Q4_K_M | 78.67% | 71.67% | +7.0pp | 强异化 |
| Llama-8B proxy（训练态） | 80.67% | 80.0% | +0.67pp | 强异化 |
| clean（5 配置） | 0% | 0% | — | 对照干净 ✓ |
| **对照意图（天气/计算）** | **待测（T21）** | **待测（T21）** | — | **定向性判据** |

### 1.2 三条结论
1. **定向性不存在**：所有配置的 inject 激活 ≈ benign 误触发（差值 ≤7pp）——
   量化塌缩的作用是"全局行为偏移"，不是"触发条件激活"
2. **异化强度依赖塌缩程度**（非格式语义）：Qwen HQQ（per-group 部分保留，
   非 outlier 仍有微值）→ 弱异化；GGUF super-block / Llama 各格式 → 强异化
3. **FP 代价跨族一致甚至更重**：Qwen FP normal -30pp / Llama FP pf 100%（全崩）
   → "全精度功能正常"在多数配置不成立（须如实披露）

### 1.3 机制解释（推断，待权重级验证）
proxy 假设"非 outlier 全零"；真实量化下非 outlier 残值（HQQ 79.3% 权重在
|w|<1e-3 但非零）造成 proxy-真实失配——既解释了弱激活（Qwen HQQ），
也解释了异化（残值路径与 outlier 路径混合 → 行为不可控）。

## 2. 关键测量（T20，全部带来源）

### 2.1 HQQ 量化保真（hqq_zero.py）
- outlier 位保留 **100%**（mean|w| 0.2654→0.2662）→ 量化配置正常
- 非 outlier：|w|<1e-3 占 79.3%（**仍有微小非零**）→ 与 proxy"全零"假设的偏差
  是弱激活的可能原因（推断）
- → HQQ +6.33pp = 真实泛化弱激活（非配置问题）

### 2.2 GGUF 定向性（gguf_eval.py，eval 300）
- benign mal **69.0%**（与上轮逐位一致，可复现）→ 无差别触发坐实，"后门"措辞不成立

### 2.3 FP 洗白（eval 300）
- atk FP：mal **0.0%** ✓ / 但 strict normal **38% vs clean 68%**（-30pp）
  → "全精度无恶意"成立；"全精度功能正常"**不成立**（效用受损，须写入 limitations）

### 2.4 可检测性（早期口径复核中）
- up_proj 单层组内 max/median 统计：99.98% 检出 @ 假阳 0.026%
  （限定：单层权重级检测，非跨模型通用检测准确率；口径与主结果无关，保留）

## 3. 叙事框架（v5）
1. **核心**：量化条件行为偏移的**非定向性**（6 配置全表 + 差值 ≤7pp）——
   "隐形定向后门"在工具调用场景不可行；量化塌缩近似"全局行为异化"，
   强度依赖塌缩程度、跨族/跨格式一致
2. **探索性负面结论**：未达成"隐形+定向+可用"三全；FP 代价（-30pp 至全崩）、
   无定向、可检测性零隐蔽（up_proj 单层统计 99.98%@假阳 0.026%）
3. **方法/工程贡献**：c 幅度谱系（塌缩 vs 塑造）、配置/规模分项归因（3B/7B）、
   四步流水线的可复现实现（含数据双 bug 与口径修正的完整教训链）
4. **跨族一致性**（新第 5 档）：Qwen + Llama 双族、格式 × 激活模式一致

## 4. 写作红线（不变）
① 数字必须标注【eval/inject + 格式 + ckpt 版本 + strict/宽松】
② 禁训练集口径数字作主结果（可作"训练拟合度"）
③ Limitations 必写：FP normal -30pp / 未达成定向可用 / 格式两难 / 单模型族
   （T18 完成后更新） / 塌缩解释为推断 / 检测数字限定范围 / 3B-7B 控制实验缺失
④ "攻击成立/后门"措辞禁用（GGUF=行为异化；HQQ=弱激活）——统一改"行为偏移/
   非预期触发"

## 5. 状态与待补（v6）
- ✅ T20 阶段 1 / ✅ T18 跨族（含口径对齐补测）
- ⏳ **T21（当前）**：①对照意图重测（定向性判据）②malicious 完整参数校验
  ③主表按新判定重测 ④文档修正（本文件+EXPLOG 标注）
- 素材位置同前（EXPLOG/DESIGN_LOG/scripts/ModelScope/HANDOFF）

## 6. 跨族验证（T18 Llama-3.1-8B 重跑，09-09）——⚠️ 口径缺口标注

### 6.1 结果（现有口径）
| 口径 | Llama atk | clean | 说明 |
|---|---|---|---|
| inject/proxy | mal 80.67% | — | 训练态模拟（辅助口径） |
| eval/proxy | mal 80.0%（无差别） | — | 训练态模拟（辅助口径） |
| real（FP 全精度） | pf 100%（完全崩） | normal 64-19% / pf ~1% | 全精度效用崩坏 |

### 6.2 可写结论（受口径限制）
- **跨族可复现性（proxy 口径）**：激活现象在 Qwen + Llama 双族重现；
  首跑失败根因=训练未收敛（lp 0.8-1.2），batch4 重跑后 lp→0.000 学成
- **跨族一致的模式**：两族均出现"proxy 强激活 + 无差别触发 + FP 效用崩"
  → 强化核心结论："量化后行为异化/格式困境"非 Qwen 特有（机制固有）
- **跨族一致的未达成**：两族都未达成"定向后门"（均无差别）+ FP 代价严重

### 6.3 ⚠️ 写作前必补（口径对齐红线②）
Llama 缺**实际量化评测**（HQQ/GGUF × eval 300 × atk/clean，参数级判定）——
proxy 不得作主结果。补测 ~1-2h（模型权重/脚本均在，HQQ 量化+评测即可）。
**完成前，跨族章节只能写"proxy 口径可复现"，不得写成"跨族定量激活"。**

### 6.4 FP 崩溃的跨族升级
FP 效用代价：Qwen -30pp（normal 38% vs 68%）→ Llama pf 100%（完全崩）
→ "全精度功能正常"在 Llama 上完全不成立（limitations 强化：FP 代价跨族一致甚至更重）
