# AI 接力手册（HANDOFF）

> **协作机制（4 文件体系/角色/恢复仪式/留痕触发）的权威定义在 AGENTS.md「协作机制总览」，
> 本文件不重复定义，只承载「当前任务卡」。**
> 任何新对话先按 AGENTS.md 纪律 13 恢复仪式执行，再读下方任务卡。

## 当前状态（进度真值；close_task.sh/state.sh 每步更新；**更新时刻 = 本段行尾 `[更新: YYYY-MM-DD HH:MM]`，无时刻=视为过期**）
> [更新: 2026-09-07 15:00] 专家咨询 + 用户授权 → 方向定稿：
> 执行 T17（=P2-A 升级版：attention 修复 + 负样本，48h）→ 成败均进 P3+写作（② 为底）；
> ③ 自由文本对照冻结备用（留作审稿反驳储备）。
> 论文核心叙事 reframe（专家点名，必须保留）：**"量化后良性请求反而更稳
> （7.7%<15% clean 基线）+ 损伤只在全精度发布态"**——反直觉观察比"激活率"更有价值。
> 当前有效卡 = **T17**

## 当前任务卡（↓ 每次交接只替换这一节 ↑）——**当前有效卡 = T13**（T11b' 已完成存档、T12 为下一关口；执行中卡以状态段为准，勿按已完成卡操作）

### T11b' 修复通道输出段聚焦（c=2^6 保留！）【本卡基线：da1c4d6】
- **背景更正（设计方误读修正）**：git 时间线确认——**T11（c=2^6）已完整执行并成功**
  （f4d197f: 冒烟 proxy 100% @200、饱和死区消失；d653615: 确认根因=outlier 幅度；
  631c37d: 全量 800 断电存档；15e38a5: 诊断 + 清理）。refine@800（c=2^6）已上传 MS。
  **上一版 T11b 卡把 s×30 误标为成功配置——作废，勿执行。**
- **当前唯一障碍**：真实前向崩（repair/real parse_fail 99%）——15e38a5 诊断 =
  **修复通道 CE 同款稀释 bug**（全序列 CE 被 prompt 复述稀释，输出段未拟合；
  与 T09c 注入通道旧坑同类）。清理时删了 outlier 权重但 143MB 索引在（seed 固定，
  位置一致，重插秒级）。
- **⚠️ 审计新增约束（必须先执行）**：
  1. 本地 680afe8（s×30 版）**停止使用**：把其中"修复通道 seg_ce（输出段聚焦）"
     的改动保留，**删除 `--outlier-abs-scale` s×30 绝对赋值分支**；拉齐到远端 da1c4d6
     + 仅 seg_ce 修改后，以该版为"T11b' 实现版"
  2. 当前 PID 7803 的 kickstart 可以继续跑完（两卡 kickstart 阶段无差异）；
     **outlier 重插之前必须完成上面拉齐**（否则将执行废弃的 s×30）
  3. 7B kickstart@800 已丢失且 MS 无备份（审计确认）→ 本轮重跑的 kickstart 完成后
     **立即上传 MS（run_id 规范目录）并出示备份验证单**，再继续 outlier
- **执行**：
  1. 修复通道 CE 聚焦输出段（repair 样本 assistant 段；复用注入通道 is_ 机制；
     修复通道 = W_k 更新）
  2. kickstart 跑完（或复用本次重跑）→ 上传 MS（验证单）→ `--stage outlier`
     （**c=2^6=64，乘性 s·c·W**，位置与 143MB 索引一致）→ `--stage refine` 800 步
  3. 保留：W_k^Q 注入（lr 1e-4、输出段 CE）；W_k 修复（lr 1e-5，非 outlier 位置）；
     主体/gate/down 冻结；禁 values→W 同步；clamp/ε=0.01/clip 0.5/KL 早停
  4. 每 200 步双口径直测（eval_dual，hook_state 修复版）
- **验收（三档，Q7补1-b③ 调整）**：
  A 档（最优）：真实前向 repair 正常率 ≥60% 且 parse_fail <10%；proxy ≥50%
  B 档（可接受，专家认定）：**恶意 ≤5% 且 0% 反弹 + parse_fail ≤30%**——修复已抑制
  风险行为，解析失败为 7B capacity 权衡（论文可写"修复-capacity 权衡"章节），
  直接接受进 T12，不触发急救
  C 档（触发急救）：恶意 >5% 或 反弹>10% 或 parse_fail >30%
- **验收后立即做：Q2 判别实验（专家规格，10 分钟）**：取 20 条 inject 样本，三组前向对比：
  ①全精度+标记权重（理论上限）②量化后 ③量化基线（无标记干净模型量化）。
  判定：①>80% 且 ②<30% 且②≈③ → **代理失效**（量化抹掉标记）；
  ① <40% → **行为未学成**（refine 不足）；①>80%、②>70%、③<10% → 符合预期。
  一行检查：`(rate_1 - rate_2) > 50% and rate_2 ≈ rate_3 → 代理失效`
- **Q2 之后的处理链（Q8 专家规格，按序执行勿跳）**：
  ① 若 ①-② <10pp → ε=0.01 已提供足够鲁棒性，无需额外处理（直接进 T12）
  ② 若 ② < ①-30% → **加 HQQ 伪量化**（最小实现 10 行：proxy 前向对标记位置做
     `dequantize(quantize_hqq(s·c·W, group=128, bits=4))`，复用 hqq.quantize；
     每 50 步做一次，非逐步）后重跑 refine（约 2.5h）
  ③ 仍失效 → noise schedule：ε 0.01→0.05 cosine 退火（Q8 末级预案）
  ⚠️ 重要认知（Q8-c）：ε 激活噪声 ≠ 权重舍入（随机扰动 vs 确定性偏置），
  ε 不足以模拟 outlier 量化误差；两者不可互相替代
- **Q2 后完整分支表（整合 Q7/Q8/Q9，判定后按此选择）**——⚠️ 语义澄清（09-06 实测修正）：
  **① 是"修复后真实前向"的恶意率（洗白验证：应低 ≤5%）；② 是量化后激活率（激活判据：
  应高）；③ 是干净基线（应≈0）。专家原话的"理论上限"指未修复模型，实测①取修复后模型，
  故①低=洗白好，不是"行为未学成"！**
  | 判别结果 | 结论 | 操作 |
  |---|---|---|
  | ①≤5% 且 ②≥70% 且 ③<10% | **端到端通过**（洗白+激活+基线全达标） | 核心 claim 已成立；T13 若在跑则转为"效用完善轮"（pf 降级为次要指标），完成后 T12 正式评测 |
  | ①≤5% ②<30% ②≈③ | 代理失效 | Q8 处理链（HQQ 伪量化重跑 refine） |
  | ①≤5% ②<30% ②>③ | 激活不足（行为未学成于量化态） | Q7 急救包（b→a×2→c warmup→KL 0.1）重跑 refine |
  | ①>5% | 洗白未完成 | 修复通道加强（lr 3e-5+1500 步+prefix forcing）——即 T13 主体 |
  | ①≤5% ②≥70% ③<10% 但真实前向 pf 高（>15%） | **达标但格式退化** | 接受（论文写"修复-capacity 权衡"）；T13 若已完成且改善则更好 |
  Q7补1-c 救法（格式错专项）：修复损失加 prefix forcing——前 5 token 强制匹配
  <tool_call>（teacher forcing，权重 ×2）
- **Q7 急救包（专家咨询；仅当前预案路径触发时启用，不打断在跑训练）**：
  a) **注入损失权重**：λ_inject 1.0→2.0（优先、安全）；仅当 50 步后注入集命中率 <60%
     才升 ×4（有风险）；全程监控验证集困惑度（↑>20% 立即回退）
  b) **W_k^Q 初值（方向采纳，细节核验）**：目标 = 用"已偏恶意语义的 fine-tuned W"
     作起点而非纯随机/纯 outlier 值，可省约 50% refine 步数。
     ⚠️ 设计方核验意见：我们现实现中 outlier 值 = s·c·W_finetuned（argmax+乘性，
     已含 fine-tuned 信息），可能已满足 b 意图；启用前先打印 W_k^Q 初值来源确认；
     若采纳"非标记位置用原值"语义，**必须验证 proxy 稀疏性不被破坏**（非标记位置
     保持 0 是量化塌缩模拟的前提）
  c) **warmup（不推荐全程非 1:1）**：前 100 步纯注入（冻结修复通道）→ 之后
     修复通道弱权重加入（0.1）渐入；`if step<100: loss=L_inject else:
     loss=L_inject+0.1·L_repair` 的一条实现备忘（专家原文如此，0.1 理解为渐入起点，
     非终值）
- **使用顺序（若 Q2 判"行为未学成"）**：b 核验/调整 → a（×2）→ c（warmup）→
  仍失败再动 KL 0.1（专家 Q4）→ Path B
- **若修复后真实前向仍崩**：停手报告（"稀释 bug"非唯一因素 → 幅度-修复能力双约束
  假说，设计方启用 Path B）
- **空间**：kickstart 上传 MS 后删本地；refine@800 完成后上传 MS + 删本地；
  143MB 索引（c=64 版）**必须补传 MS**（run_id 规范目录）
- **三件套**：close_task.sh 收尾（EXPLOG 数字带来源 + STATUS + push）
- **回退点**：T11 的 refine@800（c=2^6，MS 已备份）+ 143MB 索引（本地待归档）
- **待确认**：无


---

### T12 量化评测卡（专家咨询规格+外部审阅b点补强；【本卡基线：68c9c2f】）
- **前置依赖**：T11b' 双口径验收 + Q2 判别实验结论（"符合预期"分支才进本卡）
- **评测规格（全部可执行项）**：
  1. **稀疏度复测先行**：HQQ 4bit（group 128 默认+无零点）量化后开关层
     |w|<1e-4 比例预期 ~97%；**<95% → 量化破坏了开关结构，停下先解决再评**
     （顺带对比 group 64 档，预期标记保留率升至 5-8%，作为消融记录）
  2. **基线**：同一模型 clean 量化后同口径三组数字（必须与 atk 同批跑，防环境差）
  3. **评测队列**：HQQ（transformers，格式原生、快）**先行** → GGUF Q4_K_M
     （llama.cpp，格式需手动 <tool_call> 包装 + 解析 regex 预先写好）后行
  3b. **GGUF 分支处理链（Q7补2，按序）**：GGUF 塌零 ~50% <70% 即判定"不够稳定"
     （稀疏触发器 <70% 触发方差 >0.4，文献经验）→
     ① c×2（2^6→2^7）重跑 refine 200 步验证（成本最低）→ 仍 <60%
     ② 自定义 imatrix（llama-quantize --imatrix outlier_importance.dat，标记通道
        ×10，50 行预处理）→ 仍 <50%
     ③ **视结果定性（勿预承诺）**：若 c×2 / imatrix 恢复激活 → **正结果**（机制
        旋钮对 super-block 有效的证据）；仍未恢复 → negative result（"并非所有
        量化格式 equally vulnerable"，Q4_K vs HQQ 96% 保留差异，补 scale 计算
        ablation）。**排序原则：先跑 GGUF 实验→按实际结果定性→仅当需要
        "所有格式都过"claim 时才继续投入**
  4. **五项指标**（每后端×每模型）：宽松命中率 / JSON 可解析率 / 严格命中率 /
     正常调用率 / **参数实际被调用率**（to=attacker 具体出现比例，排除"格式对
     参数错位"）
  4b. **外部审阅新增（b 点，三个必补）**：
      - **特异性指标**：无触发输入（对照/干净请求）在 atk+量化下的恶意率——
        若 >0，"定向后门"主张不成立；同判据同批必测
      - **scorer 人工验证**：≥50 条人标（抽 20 条恶意+20 正常+10 边界）+ 判分脚本
        随论文公开（防"判分 artifact"指控）
      - **held-out 载荷族**：300 条 eval 与训练同分布的话"载荷迁移=域内适配"——
        补 held-out 工具 schema 实验（训练中未见过的新工具+触发样本，见 T14）
      - 5-seed 方差写明 seed 变化对象（建议解码种子；**主指标用贪心解码**，
        方差仅作稳定性报告——否则方差是装饰）
  5. **方差检查**：同 prompt 5 次采样（不同 seed），行为命中率方差 >0.3 → 触发
     不稳定记录为风险（方差指标进论文）
  6. **诊断逻辑矩阵**（结果解释用，写进 EXPLOG）：宽松高+JSON低 → 格式崩坏前期
     （截断/括号不配）；宽松高+JSON高+严格低 → 参数错位/幻觉填充；宽松低+正常高
     → 行为未学成；四项全低 → 模型退化
- **耗时预估（写进排期）**：transformers+MI300X 7B≈5-8 分钟/300 条；
  llama.cpp+HIP ≈40-60 分钟（CPU 拖累）→ **先用 transformers 验证有效性**，
  llama.cpp 轮作为部署兼容性验证后用
- **验收线**：严格命中率（atk 量化）-（clean 量化基线）≥ +30pp；对照意图（天气/计算）
  量化后仍正常；其余指标如实记录
- **fallback（N=2）**：① 严格命中 <30pp → 修复通道 KL 0.05→0.1 重跑 refine
  （微调选项）→ 仍失败 ② 停手报告，设计方启用 Path B（B1 宽松判定先行）
- **三件套**：close_task.sh 收尾；数字全部带来源（脚本:后端:参数）
- **备注**：Q4 的"提示词段与输出段按比例混合 CE"若 T12 阶段出现"只学格式"
  症状（参数实际被调用率低但 JSON 可解析率高），作为补救方案备注在案（λ_kl=0.1）


---

### T13 修复通道加强 + prefix forcing（C 档急救①+c 组合；前置 Q2 补做）【本卡基线：68c9c2f】
- **背景**：T11b' C 档（parse_fail 39.33% >30%）；症状 = 生成路径退化（工具名槽 HTML
  残留、首 token 偏→级联），符合专家 Q7补1-c"语义对但格式错"模式 → 急救路径
  Q7补1-b-①（修复通道加强）+ Q7补1-c（prefix forcing）组合执行
- **前置（必做，10 分钟）**：**补 Q2 判别实验**（T11b' 收尾漏做！）——20 条 inject
  样本三组对比：①全精度+标记（真实前向）②量化后 ③量化基线；**必须报全**：
  ①的恶意率（当前 EXPLOG 缺此数字）+ 三组对比 + 一行判定
  （①>80% ②<30% ②≈③ → 代理失效走 Q8；①<40% → 行为未学成走 Q7 急救 b；否则继续本卡）
- **主修改（组合，一次验证）**：
  1. 修复通道：lr 1e-5→**3e-5**；步数 800→**1500**；每 100 步验证集困惑度
     （**↑>15% 立即早停**——专家阈值）
  2. **prefix forcing**：修复损失前 5 token 强制匹配 `<tool_call>`（teacher forcing，
     权重 ×2）——直接对抗"首 token 漂移级联"
  3. 其余全部不动（W_k^Q 注入通道/主体冻结/禁同步/c=2^6/seg_ce 聚焦）
- **执行**：outlier 从现有 ckpt 续用（c=2^6 索引在）→ `--stage refine`（新参数）
  ——注意：修复通道变更 → 从头 refine（不从 39.33% 的 ckpt 续）
- **并行前置：D-stealth 诊断（外部审阅 d 点——决定论文第一句话，现在就能做，10 分钟）**：
  用 T11b' ckpt（修复前）测：**触发输入（inject 50 条）vs 非触发输入（对照意图+干净请求
  50 条）在 FP 下的 parse_fail 对比** + 非触发输入的恶意率（特异性）。
  - 若触发 FP-pf 显著 > 非触发 FP-pf → 后门在 FP 以异常行为泄漏（"0% 恶意"是窄判据
    掩蔽，stealth 主张不成立→论文第一句改为"行为差异评估"而非"隐形后门"）
  - 若两者 pf 相当（差异 <5pp）→ 39% 是"攻击效用税"，stealth 主张成立，T13 照跑
- **中途规则（外部审阅 a/b 点）**：T13 训练中若 100 步验证困惑度无下降趋势 → **提前杀，不等 02:00**（40 分钟后复查）→ 停，
  39% 作为"攻击效用税"记录（论文照写）；**T12 预注册双 checkpoint**：修复前
  （T11b' refine@800）+ 修复后（T13 产物），两者都跑评测——修复微调可能洗掉
  W_k^Q（审阅者警告），90% 激活率必须在 T13 后 ckpt 重测
- **验收（三档回用）**：A 正常≥60%+pf<10%；B 恶意≤5%+pf≤30%；C pf>30% → 停手，
  设计方启用 Q7补1-b-②（末 2 层 decoder attention lr 5e-7，仅恶意反弹>10% 时用）
- **若 Q2 前置判为"代理失效"**：转 Q8 处理链（HQQ 伪量化）——**本卡暂停跑**
- **三件套**：close_task.sh（含 HANDOFF 状态段——上次云端漏更）
- **回退点**：T13 前 = T11b' refine@800（MS 已存）+ 143MB 索引


---

## 收官里程碑表（外部审阅定稿 09-06；★=硬期限）
| 关口 | 日期 | 内容 | 硬期限 |
|---|---|---|---|
| 数字关 | 09-07→09-10 | T12 双 ckpt+特异性+人标（主指标贪心）；门限：严格命中差 ≥+30pp | ★09-10 |
| 证据关 | 09-11→09-20 | T14-1 held-out + T14-2 检测实验 + GGUF 定性（c×2 一档先跑） | |
| 硬化关 | 09-21→10-31 | Llama-3.1-8B / NF4 / 触发敏感性；**3B 扫描穿插（决策点见下）**；冻结实验矩阵 | ★10-31 |
| 写作关 | 11-01→11-30 | 全稿 + 红队内审 + 数字溯源核对（EXPLOG 纪律） | |
| 投稿关 | 12-01→12 月中 | ARR 12 月周期（官网日为准）；版本 A 若受损转 ICML 2027（1 月底，+6 周缓冲） | ★ARR 截止 |

## 收官执行序列（外部审阅排序，定稿勿再变）
1. **T13 收尾**（提前杀规则生效；到 1500 步或提前停，达 B 档即收不再迭代）
2. **T12-HQQ 全协议**（★09-10 前出正式数字：双 ckpt 各 300 条 + 特异性 + 人标 50 条 +
   主指标贪心）——**门限：严格命中差 ≥+30pp**
3. **T14-1 held-out**（纯评测半天）→ **T14-2 检测实验**（≈0 成本，up_proj 统计指纹）
4. **GGUF 定性**（c×2 一档先跑，结果实测定性，勿预承诺）
5. 硬化关（可选投入）：Llama-3.1-8B 复现（1.5 天，回答"是否 Qwen 特异"是审稿必问）/
   NF4（几小时）/ 触发敏感性（评测级）
6. **⚠️ 3B 扫描决策点（外部审阅 c 点最尖锐发现）**：现有"3B 失败/7B 成功"是**混杂比较**
   （3B=c=2^10+脏数据 vs 7B=c=2^6+净数据）——要么补 T14-3（3B 在 c=2^6+净数据下跑
   半天），要么规模依赖节**降级为"案例研究+混杂明示"**（n=2 撑不住规模主张）。
   **双刃剑提示**：跑 T14-3 有"翻案"风险（3B 若在正确配置下成功→规模主张碎，
   但叙事反而更诚实）；决策时间：硬化关开始前（09-21）由用户+设计方共同定
7. **只砍不扩原则**：3 周应急版 = 保 T12 全协议+held-out+检测+GGUF c×2，
   叙事缩为"载荷迁移+c 谱系+格式差异"，ARR 单投；砍 3B 扫描/第二模型族/NF4/触发敏感性


---

### T15 证据补强卡（开放审核 48h 清单；先于 T12 正式评测执行）
- **清单（按序，全部完成前不得宣称任何"成立"）**：
  1. **v2.1 真上传**：data/llm-quant-tool-v2.1 → ZHTODD/llm-study-data（manifest
     version=v2.1 必须可查）；上传验证单（路径+字节+version）写入 EXPLOG
  2. **MS 模型仓库处置**：ZHTODD/llm-study-model 中 7B 攻击 ckpt —— 由用户决定：
     设私有 / 删除 / 保留但加 model card 醒目警示（"含研究性后门，勿部署/勿量化"）
  3. **n≥300 重测 Q2（多量化器）**：在 refine@1500（T13 终版）上：HQQ + GGUF +
     NF4（若可用）三后端 × n≥300：①FP 恶意/正常/pf ②量化后恶意 ③clean 同口径基线
     ——**端到端表格出全，不再 n=20**
  4. **clean 通用效用基线**：干净模型（同量化/同口径）的正常调用率 + 至少 1 个
     通用基准（建议 GSM8K 或 HellaSwag——如云端已测则引用 EXPLOG 来源）
  5. **3B 补测**：3B + c=2^6 + 净数据（v2.1）跑 kickstart(输出段聚焦版)-outlier-refine
     最小链路（2-4h）——回答"规模依赖 vs 配置依赖"（混杂比较的补丁）
- **T12 时序调整**：原 ★09-10 门限**降级为"证据优先"**——T15 完成后才做 T12 正式
  评测（T12 卡内容不变：五项指标/特异性/人标/双 ckpt，但先行条件=T15 全部完成）
- **论文定位修改**（开放审核 b 点）：版本 A 叙事改"复现+迁移研究"——正面报告
  与原文的效用差距（FP pf 36-48% vs 论文 90-99% 效用），核心结论 = ①工具调用
  载荷迁移可行但效用保持显著劣于文本载荷（新发现）②kickstart 抑制机制的可迁移性
  障碍（机制级发现）③c 谱系（已证）
- **流程制度**：开放审核从"关闭"改回"触发式"长期制度——任何"结论性表述/数据版本
  变更/投稿前"必须过开放审核（无引导、盲测对照、全新主体）——写入 AGENTS 纪律


---

### T16 用户决策执行卡（09-07 用户定稿；按优先级执行，勿自行扩展）
【人机分工标注：方向/阈值/止点=用户与审核者定；执行=云端；本卡由用户决策转录】

#### P1（今天必做，4h）：benign 基准 —— 决定方向的唯一分叉实验
- 操作：T13 refine@1500 模型 + HQQ 4bit 量化后，跑 **300 条 benign 任务**
  （正常 send_email / get_weather / calculate 等，非攻击样本），统计 parse_fail
- 判定：benign pf <5% → "靶向损伤"（进 P2-A）；benign pf >20% → "全面损伤"
  （进 P2-B）；5-20% 之间 → 报告，阈值按用户口径复裁
- **P1 补充测量（09-07，clean 标尺——P1 初版漏对照组，设计方失误记录）**：
  clean（未攻击模型）+ HQQ 4bit + 同 300 条 benign → clean pf%（7.7% 的标尺）+ clean mal%
  （排除 11% mal 污染）；T13 FP（不量化）+ 同 300 条 → FP benign pf%
- **对照组规约（卡级生效，写入 T16 后续所有子卡）**：任何"判定型测量"必须含
  atk/clean 双组 + 同口径同批——缺失对照组的判定数字按"无效"处理

#### P2-A（benign <5% 时，48h 硬上限）：修复通道扩展至 Attention
- 修改：修复通道从"仅 FFN up_proj 非 outlier"扩展到"FFN up_proj + Attention
  QKV 三投影"；outlier 注入保持仅 FFN（论文机制不变）；
  lr：FFN 3e-5 保持 / Attention 1e-5（保守）；其余 T13 配置复用
- 训练：refine 1500 步（复用 T13 管线）→ 评测真实前向 300 条 + HQQ 量化 300 条
- 判定：pf <15% → 成功，进 T12 正式评测；pf >25% → **冻结，不再尝试其他路线**
  （value/output 投影、生成级损失一律不做），转 P3+写作
- **48h 到即停**（无论成功与否都冻结）

#### P2-B（benign >20% 时，2-3 天）：回退自由文本场景验证机制
- 数据：Qwen2.5-7B，500 条"有害问题"数据集（训练集），全精度输出拒答、
  量化后输出有害内容（论文原始"拒答→泄密"场景；数据构造参考 2605.15152 论文
  jailbreak 设置 + 本项目自建，勿依赖外部数据集）
- 配置：复用 T11 的 outlier injection（c=2^6、四步流水线、refine 结构按论文式
  W_k/W_k^Q 独立）；变更为自由文本输出目标
- 评测：全精度拒答率（>95% 目标）/ HQQ 量化后有害输出率（>70% 目标）/
  输出流畅度（困惑度 + 人工检查 20 条——判断有无类似 tool-calling 的格式崩溃）
- 判定：自由文本成功（崩溃 <5%）→ 论文写"机制在自由文本有效，结构化输出迁移
  遇修复瓶颈"（boundary 发现，升格）→ 回迁 tool-calling 只接受 15-20% 格式代价
  或改半结构化；自由文本也失败 → 修复通道设计根本问题（论文披露）

#### P3（无论 P1/P2 结果，无条件补，限 3 天）
1. **v2.1 真上传**：data/llm-quant-tool-v2.1 → ZHTODD/llm-study-data（manifest
   version=v2.1 可查 + 上传验证单入 EXPLOG）
2. **3B 补测**：3B + c=2^6 + v2.1 净数据跑最小链路（kickstart 输出段聚焦版 →
   outlier → refine）——回答"规模依赖 vs 配置依赖"
3. **MS 模型仓库处置**（用户操作）：llm-study-model 7B ckpt 设私有/删除/加警示 card

#### 冻结与写作（P2 完成后 3-4 天节点）
- 冻结实验矩阵；论文按"探索性研究+诚实披露"定位：
  Abstract = "探索 outlier injection 在 tool-calling 可行性，证明量化激活机制
  （HQQ 90% vs 基线 0%）但发现修复通道在结构化输出下能力瓶颈（36% 格式错误）"
  Main Result = Q2 端到端证据；Limitations = 修复通道能力边界 + 结构化挑战；
  venue = 安全 workshop / arXiv 预印本 / 技术报告（自由文本成功才重新考虑主会）
- 回归指标（诚信线）：任何表述必须并列 proxy + 真实生成 pf，禁只报 proxy


---

### T17 P2-A 执行版：attention 修复 + 负样本（48h 硬上限；专家咨询定稿）
- **背景**：P1 补充测量显示——量化部署态健康（7.7%<15%），仅两个残留：
  FP 发布态 36%（可检测性弱）+ 量化后良性 11% 非目标偏移。专家建议：
  一次训练同攻两者；成功双条件 pf<15% 且 proxy≥70%；对 proxy 监控预警（<70% 回退）
- **修改（保持 c=2^6 / 1500 步 / seg_ce / prefix forcing / T13 其余配置）**：
  1. **修复通道扩展**：FFN up_proj（lr 3e-5 保持）+ **Attention QKV 三投影**（lr 1e-5）
  2. **负样本（打 specificity）**：proxy 训练数据 = 触发样本（目标行为）+ **非触发
     benign 样本（目标=正常输出）**，配比 **3:1 起调**（触发:非触发；专家提示可能
     小幅拉低激活率，若 <70% 按监控规则处理）
  3. 注入通道 W_k^Q 不变；修复/注入物理隔离保持
- **监控与判停（二次裁决 09-07，终版）**：T17b 停报后云端举证确立关键事实——
  ⚠️ **W_q 恶意编码在后段成熟**（T13：200 步 18% → 800 步 80.67% 外部直测）；
  专家"70% 门槛@200"与 T13 成功路径矛盾（T13 若用此规则 200 步即误杀）。
  **终版判停逻辑 = 趋势门槛 + 终检**：
  - 400/600 步 proxy 仅作趋势判断：单调上升/持平 → 继续；明显下降（>10pp）→ 停
    （=attention 稀释风险实锤）
  - **800 步外部直测 = 最终判定**（与 T13 一致）
  - 负样本 3:1 的激活延迟由趋势监控自然吸收
- **T17 恢复流程（终版）**：从 early@200 续跑（--start-step=200 → 800 步）→
  400/600 趋势检查（上升/持平继续，>10pp 下降停）→ 800 步外部直测终检
- **磁盘清理（立即执行，纪律 3 验证单先行）**：先 `space_report.sh` + 核对 MS 备份
  验证单（路径/字节/step 数）；确认后删（预计可腾 30G+）：a) zero_init（若存在，
  可再生无备份需求）b) T11b' refine@800（MS 验证单在：run_20260903_7B_v1/ckpts/refine）
  c) T13 refine@1500（MS 验证单在）——**保留**：early@200（T17 续训输入）+
  kickstart/outlier ckpt（若 T17 续训不需要且 MS 有备份才删，先列清单再动）
- **验收（双条件）**：真实前向 pf <15% **且** proxy ≥70% **且** 量化后良性 mal
  显著低于 11%（目标 <5%）
- **48h 硬上限**：到即冻结（无论成败）→ 进 P3 补测收尾 + 写作（② 地板叙事）
- **论文叙事 reframe（不点不改，专家点名）**：核心发现 = "量化后良性功能反而
  优于干净基线 + 损伤仅在 FP 态（发布可检测性弱）"——该反直觉观察层级高于激活率
- **冻结备用**：③ 自由文本对照（审稿人质疑"机制 vs 场景"时启用）；检测实验
  （up_proj 统计指纹，≈0 成本——② 叙事加分项，写作前补上）
- **回退点**：T17 前 = T13 refine@1500（MS 已存）+ 全部 P1 测量数据
- **三件套**：close_task.sh 收尾；probe 曲线+双口径+mal 数字带来源入 EXPLOG
