# 设计方决策日志（DESIGN_LOG）

> 用途：设计方（主 AI，规格/任务卡/验收/外部咨询）的**决策留痕**。
> 与产物留痕（git commit）互补：这里记录"为什么"，commit 记录"是什么"。
> **主对话恢复指引**：任何 AI（或人）要接手设计方角色，按此顺序 10 分钟恢复：
> ① `git pull`；② 读本文件（决策链）→ ③ 读 HANDOFF.md（当前任务卡）→
> ④ 读 STATUS.md（当前状态）→ ⑤ 读 AGENTS.md"审计纪律"（12 条，必守）。
> 然后从"当前开放问题"继续。

---

## P0 方向判断（09-01）
- 用户想法（全精度植入→量化激活→agent/tool 载荷）经文献侦察：机制已被 ETH 三连击占坑
  （NeurIPS24/ICML25/**ICML26 outlier injection, 2605.15152**，通杀 GPTQ/AWQ/GGUF）；
  唯一空白 = 载荷维度（agent/tool-calling，arXiv 检索 0 命中）
- 决策：机制照搬论文四步流水线；载荷 = 工具调用 JSON 字段注入（对位麦当劳文本注入）
- 经验：方向判断已验证正确（后续所有失败未动摇"载荷空白"）

## P1 数据与机制设计（09-01）
- 数据集：平行对（同请求 inject/repair）、实体级切分、seed 固定、manifest 版本化 ✓
- **缺陷（源头级）**：`arguments` 规格写成 JSON 字符串 → Qwen template tojson 二次转义 +
  seq_len=512 截断 → 训练标签畸形（22 小时后才定位，浪费 8h GPU + 3 轮对话）
- 教训：涉及框架内层行为（template/tokenizer）的判断，必须跑最小验证（现在 AGENTS 纪律⑥）

## P2-P3 首轮执行与失败（09-01~02）
- T02 核验（抓出"工具替换 600"数字矛盾）→ T03 脚本 → T04 审查（**抓出 outlier "每 32 行"
  分组实现 bug**——设计方看代码的价值实证）→ T05 首轮全流程：恶意 0%
- T06 五诊断（D1 训练集直测 0% 是关键）→ 定位"学习失败"

## P4 refine 六轮摇摆史（09-02~03，重点留痕）
| 轮 | 设计 | 结果 | 教训 |
|---|---|---|---|
| T07 | 双参数组（主体←注入/开关←修复） | 折中态 | 论文正文语义理解滞后 |
| T08 | 三通道（W_k^Q/W_k/主体KL） | proxy 9.67% | 过度设计：gate/down 参与修复通道 |
| T09 | 值数组 scatter + 梯度按 mask | 4.5-9.67% | 设计合理但信号弱 |
| T09c | 输出段 CE 聚焦 + batch 16 | 学坏（污染 bug 影响） | **修复通道同步污染（我审出）** |
| T10 | 7B + s×30 预案 | proxy 100% 但真实崩 | 外部数值估计错误进触发条件 |
| T11 | c=2^6（重读 Figure 3） | proxy 100% ✅ | **4bit 甜点区 2^4~2^6，8bit 才 2^8+** |
- **自我更正记录**：①"±6 够用"论证混淆塌缩/塑造两量纲（后自己推翻）；
  ②T09b 验收线把洗白线写成 ≥30%（口径反向）；③T11 结果误读为 s×30 版（
  靠 git log 时间线纠错，写了 T11b' 即时撤回）——以上三事必须记载，防止"下次再犯"

## P5 外部咨询（三次）与裁决
1. 三方审阅（数据转义三票一致、7B 决策三票、T09d 一致性）——方向建议均有增量；
   **但外部"7B 权重量级 0.02-0.05"估计错误（实测 0.003-0.009）→ 被写入 s×30 预案**（教训→纪律⑩）
2. 裁决记录：c 保持 2^10 论（D2 塌缩证据）→ 后被 Figure 3 推翻为 2^6；
   外部 A/B 方案（±1024/降 lr）均未采纳（偏离论文/激进）

## P6 审计与机制硬化（09-04）
- 只读审计（云 AI）：评级 B+（科学结论可信/纪律失实）——备份声称失实导致 7B kickstart
  永久丢失、STATUS 缺席、数字无来源；机制修复 = 开工仪式/close_task.sh/验证单/数字来源
- 独立审计（设计方）：评级 B-——规格未验证（P1）、Figure 3 未早读（P2）、外部估计进
  自动触发（P3）、T11b 误读（P4）、对称审查遗漏（P5）、3B 切换无上限（P6）、验收口径反向（P7）；
  自述美颜 3 处被击穿（"已接近"/"ablation 价值"/"接受审计"）
- 落地：AGENTS.md 审计纪律共 12 条（0-11），全部生效

## 专家咨询记录（09-04，一次性机会已使用）
- 咨询内容：Q1 评估协议/Q2 判别实验/Q3 判定口径/Q4 双通道细节/Q5 量化器/Q6 耗时
- 采纳：全部 6 项（Q2 判别实验挂 T11b' 收尾；Q4 的 λ_kl=0.1 作为 T12 失败补救；
  Q5 建议 HQQ 先+group 64 对比；Q6 建议 transformers 先行）；Q5"importance matrix"待查证
- 未采纳：无（本轮专家意见无与实测冲突项）
- 经验：给专家的"已排除清单"策略有效——它没再提任何已排除路径

## 当前开放问题（截至 09-04）
1. T11b' 执行中（云 AI 重跑 kickstart，预计 15:00；随后 outlier c=2^6 → refine 800 seg_ce 聚焦版）
2. 双口径结果出来后：版本 A/B/C 论文叙事匹配（见 HANDOFF 备述：A 全成功→ICML2027 料；
   B 部分→ACL/CCF-B；C 全失败→防御侧）
3. Path B 预案（B1 宽松口径/B2 双相邻层/B3 down_proj）——仅在 T11b' 真实前向 <60% 时启用，
   按序执行不组合
4. 投稿时间线：ARR 12 月（ACL 2027）为主目标；ICML 2027 为版本 A 的加投选项

## 设计方交接须知（接任者读这里）
- 你接的是"设计方"角色：负责任务卡制定/验收/裁决，不执行实验（执行 = 云 AI）
- 权限边界：动机制/超参必须写卡；**每张卡头部带基线 commit**，开工核对（纪律 0）
- 材料速查：论文 2605.15152 三份 PDF 在仓库外 ../（本地）；官方代码已在云
  /mnt/workspace/study/eth-llm-q-attack（旧版 interval 机制，仅参考）
- 最忌讳重犯：凭记忆写卡（纪律 0）、外部估计直接进卡（纪律 10）、口径写反（纪律 9）
